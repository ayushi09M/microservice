apiVersion: "apps/v1"
kind: StatefulSet
metadata:
  name: "productService"
  namespace: "rtb"
  labels:
    app: "productService"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: "productService"
  template:
    metadata:
      labels:
        app: "productService"
    spec:
      containers:
        - name: "productService-1"
          image: "gcr.io/gcp-rtb-429519/productService:latest"
          ports:
            - containerPort: 8083
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "productService-svc"
  namespace: "rtb"
spec:
  #type: "ClusterIP"
  type: "NodePort"
  selector:
    app: "productService"
  ports:
    - protocol: "TCP"
      port: 80
      targetPort: 8083
      nodePort: 30061
      
